def sharpe(returns):
    return returns.mean() / returns.std() * (252 ** 0.5)import logging
from config import Config

logging.basicConfig(level=Config.LOG_LEVEL)
logger = logging.getLogger(__name__)
